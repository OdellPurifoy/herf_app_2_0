<p style="color: green"><%= notice %></p>

<h1>Events</h1>

<div id="events">
  <% @events.each do |event| %>
    <%= render event %>
    <%= link_to "Edit this event", edit_event_path(event) %>

  <% end %>
</div>

<%= link_to "New event", new_lounge_event_path %>
