<p style="color: green"><%= notice %></p>

<h1 class="is-size-3 pb-3"><%= @events.first.lounge.name %> Events</h1>

<div id="events">
  <% @events.each do |event| %>
    <%= render event %>

  <% end %>
</div>

<%= link_to "New event", new_lounge_event_path %>
