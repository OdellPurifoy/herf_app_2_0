<h1>Editing event</h1>

<%= render "form", event: @event %>

<br>

<div>
  <%= link_to "Show this event", edit_event_path(@event) %> |
  <%= link_to "Back to events", lounge_events_path([@event, @event.lounge.id]) %>
</div>
